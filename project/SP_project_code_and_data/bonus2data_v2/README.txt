1. 窄带(nb)和宽带(wb)各给了一组已知声源DOA的数据用于调试，以及各一组test数据；
2. 所有数据均包含2个声源；
3. 已知声源DOA的数据文件命名：2个声源的角度由 "_" 分开，每个声源的azimuth(θ)和elevation(φ)由 "&" 分开，θ在前，φ在后。
友情提示：注意负号

update: 这一版窄带的数据做了更新，仅对频谱特征做了更正，DOA角度的定义和结果均没变化。